# Where are all the schema files located? globs are supported eg  src/**/*.graphqls
schema:
  - graph/*.graphqls
  - internal/**/*.graphqls

# Where should the generated server code go?
exec:
  package: graph
  layout: single-file # Only other option is "follow-schema," ie multi-file.

  # Only for single-file layout:
  filename: graph/generated.go

  # Only for follow-schema layout:
  # dir: graph
  # filename_template: "{name}.generated.go"

  # Optional: Maximum number of goroutines in concurrency to use per child resolvers(default: unlimited)
  # worker_limit: 1000

# Uncomment to enable federation
# federation:
#   filename: graph/federation.go
#   package: graph
#   version: 2
#   options:
#     computed_requires: true

# Where should any generated models go?
model:
  filename: graph/model/models_gen.go
  package: model

  # Optional: Pass in a path to a new gotpl template to use for generating the models
  # model_template: [your/path/model.gotpl]

# Where should the resolver implementations go?
resolver:
  package: graph
  layout: follow-schema # Only other option is "single-file."

  # Only for single-file layout:
  # filename: graph/resolver.go

  # Only for follow-schema layout:
  dir: graph
  filename_template: "{name}.resolvers.go"

  # Optional: turn on to not generate template comments above resolvers
  # omit_template_comment: false
  # Optional: Pass in a path to a new gotpl template to use for generating resolvers
  # resolver_template: [your/path/resolver.gotpl]
  # Optional: turn on to avoid rewriting existing resolver(s) when generating
  # preserve_resolver: false

# Optional: turn on use ` + "`" + `gqlgen:"fieldName"` + "`" + ` tags in your models
# struct_tag: json

# Optional: turn on to use []Thing instead of []*Thing
# omit_slice_element_pointers: false

# Optional: turn on to omit Is<Name>() methods to interface and unions
# omit_interface_checks: true

# Optional: turn on to skip generation of ComplexityRoot struct content and Complexity function
# omit_complexity: false

# Optional: turn on to not generate any file notice comments in generated files
# omit_gqlgen_file_notice: false

# Optional: turn on to exclude the gqlgen version in the generated file notice. No effect if `omit_gqlgen_file_notice` is true.
# omit_gqlgen_version_in_file_notice: false

# Optional: turn on to exclude root models such as Query and Mutation from the generated models file.
# omit_root_models: false

# Optional: turn on to exclude resolver fields from the generated models file.
# omit_resolver_fields: false

# Optional: turn off to make struct-type struct fields not use pointers
# e.g. type Thing struct { FieldA OtherThing } instead of { FieldA *OtherThing }
# struct_fields_always_pointers: true

# Optional: turn off to make resolvers return values instead of pointers for structs
# resolvers_always_return_pointers: true

# Optional: turn on to return pointers instead of values in unmarshalInput
# return_pointers_in_unmarshalinput: false

# Optional: wrap nullable input fields with Omittable
# nullable_input_omittable: true

# Optional: set to speed up generation time by not performing a final validation pass.
# skip_validation: true

# Optional: set to skip running `go mod tidy` when generating server code
# skip_mod_tidy: true

# Optional: if this is set to true, argument directives that
# decorate a field with a null value will still be called.
#
# This enables argumment directives to not just mutate
# argument values but to set them even if they're null.
call_argument_directives_with_null: true

directives:
  autofilter:
    skip_runtime: true
  paginable:
    skip_runtime: true

# This enables gql server to use function syntax for execution context
# instead of generating receiver methods of the execution context.
# use_function_syntax_for_execution_context: true

# Optional: set build tags that will be used to load packages
# go_build_tags:
#  - private
#  - enterprise

# Optional: set to modify the initialisms regarded for Go names
# go_initialisms:
#   replace_defaults: false # if true, the default initialisms will get dropped in favor of the new ones instead of being added
#   initialisms: # List of initialisms to for Go names
#     - 'CC'
#     - 'BCC'

# gqlgen will search for any type names in the schema in these go packages
# if they match it will use them, otherwise it will generate them.
autobind:
#  - "github.com/Sanaruca/condominio/graph/model"

# This section declares type mapping between the GraphQL and go type systems
#
# The first line in each type will be used as defaults for resolver arguments and
# modelgen, the others will be allowed when binding to fields. Configure them to
# your liking
models:
  Unidad:
    fields:
      titulares:
        resolver: true
  CuotaRegular:
    fields:
      recaudacion:
        resolver: true
      gastos:
        resolver: true
  CuotaEspecial:
    fields:
      recaudacion:
        resolver: true
      gastos:
        resolver: true
  CuotaSemilla:
    fields:
      recaudacion:
        resolver: true
      gastos:
        resolver: true

  ID:
    model:
      - github.com/99designs/gqlgen/graphql.ID
      - github.com/99designs/gqlgen/graphql.Int
      - github.com/99designs/gqlgen/graphql.Int64
      - github.com/99designs/gqlgen/graphql.Int32
  # gqlgen provides a default GraphQL UUID convenience wrapper for github.com/google/uuid
  # but you can override this to provide your own GraphQL UUID implementation
  UUID:
    model:
      - github.com/99designs/gqlgen/graphql.UUID

  DateTime:
    model: github.com/99designs/gqlgen/graphql.Time

  Titular:
    model:
      - ./graph/model.Titular

  Mes:
    model: ./internal/core/common/mes.Mes
    enum_values:
      ENERO:
        value: ./internal/core/common/mes.Enero
      FEBRERO:
        value: ./internal/core/common/mes.Febrero
      MARZO:
        value: ./internal/core/common/mes.Marzo
      ABRIL:
        value: ./internal/core/common/mes.Abril
      MAYO:
        value: ./internal/core/common/mes.Mayo
      JUNIO:
        value: ./internal/core/common/mes.Junio
      JULIO:
        value: ./internal/core/common/mes.Julio
      AGOSTO:
        value: ./internal/core/common/mes.Agosto
      SEPTIEMBRE:
        value: ./internal/core/common/mes.Septiembre
      OCTUBRE:
        value: ./internal/core/common/mes.Octubre
      NOVIEMBRE:
        value: ./internal/core/common/mes.Noviembre
      DICIEMBRE:
        value: ./internal/core/common/mes.Diciembre

  EstadoDeDeuda:
    model: ./internal/administracion/models/deuda/estadodeuda.EstadoDeDeuda
    enum_values:
      PENDIENTE:
        value: ./internal/administracion/models/deuda/estadodeuda.Pendiente
      ABONADA:
        value: ./internal/administracion/models/deuda/estadodeuda.Abonada
      SALDADA:
        value: ./internal/administracion/models/deuda/estadodeuda.Saldada

  TipoDeSujeto:
    model: ./internal/unidades/models/sujeto.TipoDeSujeto
    enum_values:
      PERSONA_NATURAL:
        value: ./internal/unidades/models/sujeto.PERSONA_NATURAL
      ENTE_JURIDICO:
        value: ./internal/unidades/models/sujeto.ENTE_JURIDICO

  EstadoDeUnidad:
    model: ./internal/unidades/models/unidad/estadounidad.EstadoDeUnidad
    enum_values:
      ACTIVA:
        value: ./internal/unidades/models/unidad/estadounidad.Activa
      INHABITADA:
        value: ./internal/unidades/models/unidad/estadounidad.Inhabitada
      EXENTA:
        value: ./internal/unidades/models/unidad/estadounidad.Exenta
      EN_LITIGIO:
        value: ./internal/unidades/models/unidad/estadounidad.EnLitigio
      SUSPENDIDA:
        value: ./internal/unidades/models/unidad/estadounidad.Suspendida
      PREVENTA:
        value: ./internal/unidades/models/unidad/estadounidad.Preventa

  EstadoDeProyecto:
    model: ./internal/administracion/models/cuota/estadoproyecto.EstadoDeProyecto
    enum_values:
      ACTIVO:
        value: ./internal/administracion/models/cuota/estadoproyecto.ACTIVO
      BORRADOR:
        value: ./internal/administracion/models/cuota/estadoproyecto.BORRADOR
      CERRADO:
        value: ./internal/administracion/models/cuota/estadoproyecto.CERRADO

  MetodoDeOperacion:
    model: ./internal/finanzas/types/metodoperacion.MetodoDeOperacion
    enum_values:
      Efectivo:
        value: ./internal/finanzas/types/metodoperacion.Efectivo
      TransferenciaNacional:
        value: ./internal/finanzas/types/metodoperacion.TransferenciaNacional
      PagoMovil:
        value: ./internal/finanzas/types/metodoperacion.PagoMovil
      TransferenciaInternacional:
        value: ./internal/finanzas/types/metodoperacion.TransferenciaInternacional
      Cheque:
        value: ./internal/finanzas/types/metodoperacion.Cheque
      Compensacion:
        value: ./internal/finanzas/types/metodoperacion.Compensacion

  RolDestinoDeOperacion:
    model: ./internal/finanzas/types/roldestinoperacion.RolDestinoDeOperacion
    enum_values:
      Unidad:
        value: ./internal/finanzas/types/roldestinoperacion.Unidad
      Proveedor:
        value: ./internal/finanzas/types/roldestinoperacion.Proveedor
      Condominio:
        value: ./internal/finanzas/types/roldestinoperacion.Condominio

  Moneda:
    model: ./internal/core/common/moneda.Moneda
    enum_values:
      USD:
        value: ./internal/core/common/moneda.USD
      VED:
        value: ./internal/core/common/moneda.VED

  TipoDeOperacion:
    model: ./internal/finanzas/types/tipoperacion.TipoDeOperacion
    enum_values:
      Debito:
        value: ./internal/finanzas/types/tipoperacion.Debito
      Credito:
        value: ./internal/finanzas/types/tipoperacion.Credito

  TipoDeCuota:
    model: ./internal/administracion/types/tipodecuota.TipoDeCuota
    enum_values:
      Regular:
        value: ./internal/administracion/types/tipodecuota.Regular
      Especial:
        value: ./internal/administracion/types/tipodecuota.Especial
      Semilla:
        value: ./internal/administracion/types/tipodecuota.Semilla

  # The GraphQL spec explicitly states that the Int type is a signed 32-bit
  # integer. Using Go int or int64 to represent it can lead to unexpected
  # behavior, and some GraphQL tools like Apollo Router will fail when
  # communicating numbers that overflow 32-bits.
  #
  # You may choose to use the custom, built-in Int64 scalar to represent 64-bit
  # integers, or ignore the spec and bind Int to graphql.Int / graphql.Int64
  # (the default behavior of gqlgen). This is fine in simple use cases when you
  # do not need to worry about interoperability and only expect small numbers.
  Int:
    model:
      - github.com/99designs/gqlgen/graphql.Int32
  Int64:
    model:
      - github.com/99designs/gqlgen/graphql.Int
      - github.com/99designs/gqlgen/graphql.Int64
